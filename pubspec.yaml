name: neznakomets
description: "Незнакомец — анонимный AI-собеседник."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  riverpod: ^2.6.1
  go_router: ^14.6.2
  url_launcher: ^6.3.1
  http: ^1.2.2
  uuid: ^4.5.1
  flutter_dotenv: ^5.2.1
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.3.0
  crypto: ^3.0.3
  pointycastle: ^3.9.1
  shared_preferences: ^2.2.0
  firebase_core: ^3.0.0
  firebase_database: ^11.0.0
  device_info_plus: ^10.0.0
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/icons/
